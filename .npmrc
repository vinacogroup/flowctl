# Explicitly route @vinacogroup scope to npm public registry.
# Overrides any global ~/.npmrc that may still point to GitHub Packages.
@vinacogroup:registry=https://registry.npmjs.org
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
