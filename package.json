{
  "name": "@vinacogroup/flowctl",
  "version": "1.0.0",
  "type": "module",
  "description": "IT Product Team Workflow — MCP servers",
  "private": true,
  "bin": {
    "flowctl": "./bin/flowctl"
  },
  "scripts": {
    "setup-hooks": "node scripts/setup-git-hooks.mjs",
    "flowctl": "flowctl"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
